# Once the InferenceService reports Ready, the provider must move the Instance
# to the Ready phase.
apiVersion: core.openeverest.io/v1alpha1
kind: Instance
metadata:
  name: test-kserve-predictor
status:
  phase: Ready
