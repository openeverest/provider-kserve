# The LLMInferenceService must be garbage collected after the Instance is
# deleted. Referenced by the `error` step: if it still exists, the step fails.
apiVersion: serving.kserve.io/v1alpha2
kind: LLMInferenceService
metadata:
  name: test-kserve-llm
