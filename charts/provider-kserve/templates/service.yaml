apiVersion: v1
kind: Service
metadata:
  name: {{ include "provider-kserve.fullname" . }}
  labels:
    {{- include "provider-kserve.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "provider-kserve.selectorLabels" . | nindent 4 }}
